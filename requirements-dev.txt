# Editor-only environment for the local .venv. Nothing here reaches the
# containers -- .venv is not a bind mount. It exists so Pylance can resolve
# DAG imports.
#
# Self-contained copy of what the Dockerfile installs on top of
# apache/airflow:3.3.0, plus apache-airflow itself, which the base
# image already provides. Keep the two lists in sync by hand when
# either changes.
#
# Resync after changing it:
#   uv pip compile requirements-dev.txt -c https://raw.githubusercontent.com/apache/airflow/constraints-3.3.0/constraints-3.13.txt -o requirements-dev.lock
#   uv pip sync requirements-dev.lock

apache-airflow==3.3.0

resend>=2.35.0
beautifulsoup4>=4.15.0
apache-airflow-providers-common-ai[mcp]>=0.7
google-api-python-client>=2.198.0
google-auth>=2.55.1
